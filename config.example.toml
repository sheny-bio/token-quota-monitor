[general]
cache_ttl_seconds = 300
cache_dir = "~/.cache/tqm"
default_account = "78code"

[[accounts]]
name = "78code"
base_url = "https://www.78code.cc"
username = "shenny"
session = ""        # Run `tqm login 78code` to fill this automatically
user_id = 0         # Run `tqm login 78code` to fill this automatically

[routing]
"www.78code.cc" = "78code"
"78code.cc" = "78code"
